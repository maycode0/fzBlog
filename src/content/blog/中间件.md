---
title: '中间件理解'
description: '理解 Harness中间件的作用'
pubDate: 'Jun 17 2026'
category: AI Agent
tags:
  - LangChain
  - hello
  - 智能体
  - 中间件
heroImage: ../../assets/blog-placeholder-2.jpg
---

## 0 中间件的作用



## 1 框架自带中间件

### 1.1 总结
```python
SummarizationMiddleware(
	model=model,
	trigger=("tokens", 4000), # 单个触发条件
	# trigger=[
	# 	("tokens", 4000),("fraction", 0.5),("messages",20)
	# ], # 只要达到其中一个就触发
	# trigger={
	# 	("tokens", 4000),("fraction", 0.5),("messages",20)
	# }, # 必须同时满足
	keep=("messages", 20),# 保留的信息数
),

```

### 1.2 Human in the loop

### 1.3 model call limit

### 1.4 ool call limit

- 总的工具的调用次数
- 单个工具的调用次数
### 1.5 模型回退

### 1.6 PII检测

如果设置如让大模型看不到隐私信息，但是如果是以下情景：
```
给maycoding17@gmail.com 发一封邮件
```

则大模型无法看到该邮箱地址。

### 1.7 todolist

### 1.8 工具选择

###  1.9 工具重试

### 1.10 模型重试

### 1.11 LLM模拟工具调用

帮助在开发工具前测试模型调用的链路。
